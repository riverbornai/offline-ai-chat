module.exports = {
  dependencies: {
    // Disable React Native CLI autolinking for Expo projects
    // Expo handles autolinking through its own system
  },
  project: {
    android: {
      packageName: 'com.anonymous.speechtotext'
    }
  }
}; 